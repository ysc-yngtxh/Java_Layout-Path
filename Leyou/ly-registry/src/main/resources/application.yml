

server:
  port: 10086

spring:
  application:
    name: ly-registry

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    #如果你不想注册中心注册自己，可以进行以下配置
    fetch-registry: false          #不拉取列表
    register-with-eureka: false    #不注册服务