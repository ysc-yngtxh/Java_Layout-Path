spring.application.name=08.1_SpringBoot-TransactionManager

#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://localhost:3306/springdb?serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=131474

# 连接池类型：有 HikariCP、Tomcat JDBC Pool、Apache DBCP 等。

# 指定使用HikariCP作为连接池
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
# 连接池大小
spring.datasource.hikari.maximum-pool-size=10
# 最小空闲连接数
spring.datasource.hikari.minimum-idle=5
# 连接超时时间
spring.datasource.hikari.idle-timeout=60000