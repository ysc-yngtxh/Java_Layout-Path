<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.FindLevelTwoMapper"> <!--实体类的映射文件 namespace 指定接口的类全名-->

    <!-- 开启当前 Mapper 的二级缓存
             eviction      : 淘汰策略：最近最少使用
             flushInterval : 缓存自动刷新间隔 60 秒，会自动清空当前 Mapper 命名空间下的二级缓存。
             size          : 最大缓存对象数量
             readOnly      : 只读缓存，性能更优
     -->
    <cache eviction="LRU" flushInterval="60000" size="512" readOnly="true" />


    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultType="Student">
        select
            id,
            name,
            teacher_id AS teacherId,
            email,
            age,
            department_id AS departmentId
        from db_student
        where id = #{id,jdbcType=INTEGER}
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.example.pojo.Student">
        update db_student
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="teacherId != null">
                teacher_id = #{teacherId,jdbcType=INTEGER},
            </if>
            <if test="email != null">
                email = #{email,jdbcType=VARCHAR},
            </if>
            <if test="age != null">
                age = #{age,jdbcType=INTEGER},
            </if>
            <if test="departmentId != null">
                department_id = #{departmentId,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

</mapper>
