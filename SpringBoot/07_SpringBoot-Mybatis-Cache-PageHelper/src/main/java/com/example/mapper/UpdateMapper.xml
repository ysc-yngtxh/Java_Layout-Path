<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.UpdateMapper">  <!--实体类的映射文件 namespace 指定接口的类全名-->

    <update id="updateByPrimaryKeySelective" parameterType="com.example.pojo.Student">
        update db_student
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="teacherId != null">
                teacher_id = #{teacherId,jdbcType=INTEGER},
            </if>
            <if test="email != null">
                email = #{email,jdbcType=VARCHAR},
            </if>
            <if test="age != null">
                age = #{age,jdbcType=INTEGER},
            </if>
            <if test="departmentId != null">
                department_id = #{departmentId,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.example.pojo.Student">
        update db_student
        set name          = #{name,jdbcType=VARCHAR},
            teacher_id    = #{teacherId,jdbcType=INTEGER},
            email         = #{email,jdbcType=VARCHAR},
            age           = #{age,jdbcType=INTEGER},
            department_id = #{departmentId,jdbcType=INTEGER}
        where id = #{id,jdbcType=INTEGER}
    </update>

</mapper>
