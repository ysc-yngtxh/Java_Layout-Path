
server.port=8080
server.servlet.context-path=/

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/springboot?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=131474

mybatis.type-aliases-package=com.example.domain

# 开启 MyBatis SQL 日志，可以在控制台看到执行的 SQL 语句文本块
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
# 配置日志级别
logging.level.com.your.mapper.package=DEBUG
